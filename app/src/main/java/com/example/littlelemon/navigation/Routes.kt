package com.example.littlelemon.navigation

enum class Routes {
    REGISTER, LOGIN,
    HOME, SPLASH, SEARCH,
    DISH_DETAIL, ADDRESS_CONFIRM, SELECT_PAYMENT, CHECKOUT_REVIEW,
    EDIT_PROFILE, VIEW_ADDRESSES
}